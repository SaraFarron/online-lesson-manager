# Архитектура Online Lesson Manager

## Обзор системы

```
┌─────────────────────────────────────────────────────────────────┐
│                        СЕРВЕР 1 (основной)                      │
│  ┌─────────────┐     ┌─────────────────────────────────────┐   │
│  │  Frontend   │────▶│           Backend (FastAPI)         │   │
│  └─────────────┘     └──────────────┬──────────────────────┘   │
│                                     │                           │
│                      ┌──────────────▼──────────────┐           │
│                      │         PostgreSQL          │           │
│                      └─────────────────────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                                     ▲
                                     │ HTTPS + API Key
┌─────────────────────────────────────────────────────────────────┐
│                   СЕРВЕР 2 (другая страна)                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │   Telegram Bot (aiogram) + Cache + Circuit Breaker      │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Чеклист реализации

- [ ] Часть 1: Безопасность (аутентификация между сервисами)
- [ ] Часть 2: Health checks
- [ ] Часть 3: Система уведомлений
- [ ] Часть 4: Логирование
- [ ] Часть 5: Circuit Breaker + Cache (в боте)
- [ ] Часть 6: Бэкапы PostgreSQL
- [ ] Часть 7: Деплой

---

## Часть 1: Аутентификация между сервисами

**Цель:** Защитить API от несанкционированного доступа. Бот должен подтверждать свою личность.

### Backend

**Файл:** `app/core/config.py`
```python
# Добавить в Settings:
telegram_bot_api_key: str  # Ключ для аутентификации бота
```

**Файл:** `app/api/deps.py`
```python
# Добавить dependency для проверки ключа:
# - Читать заголовок X-Service-Key
# - Сравнивать с settings.telegram_bot_api_key
# - Если не совпадает → 403 Forbidden
```

**Файл:** `app/api/v1/endpoints/internal.py` (новый)
```python
# Все internal endpoints используют эту dependency
# Пример: @router.get("/notifications/pending", dependencies=[Depends(verify_service_key)])
```

### Telegram Bot

```python
# При каждом запросе к backend добавлять заголовок:
headers={"X-Service-Key": config.BACKEND_API_KEY}
```

### Environment Variables

```env
# Backend (.env)
TELEGRAM_BOT_API_KEY=сгенерировать-длинный-случайный-ключ

# Telegram Bot (.env)  
BACKEND_API_KEY=тот-же-самый-ключ
BACKEND_URL=https://api.example.com
```

**Как сгенерировать ключ:**
```bash
openssl rand -hex 32
```

---

## Часть 2: Health Checks

**Цель:** Понимать, жив ли сервис. Использовать для мониторинга и Docker healthcheck.

### Backend

**Файл:** `app/api/v1/endpoints/health.py`

Endpoint: `GET /api/v1/health`

Логика:
1. Проверить подключение к БД (`SELECT 1`)
2. Вернуть статус: `healthy` / `unhealthy`
3. Вернуть детали: database connected/error

Пример ответа:
```json
{
  "status": "healthy",
  "database": "connected",
  "timestamp": "2026-01-16T12:00:00Z"
}
```

### Docker

**Файл:** `docker-compose.yml`

Добавить в сервис `api`:
```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
  interval: 30s
  timeout: 10s
  retries: 3
```

---

## Часть 3: Система уведомлений

**Цель:** Отправлять напоминания об уроках через Telegram. Уведомления не должны теряться.

### Архитектура

```
Backend создаёт уведомление → Сохраняет в БД (status=pending)
                                      ↓
Бот каждые 30 сек: GET /internal/notifications/pending
                                      ↓
Бот отправляет в Telegram → PATCH /internal/notifications/{id} (status=sent)
```

### Backend

**Файл:** `app/models/notification.py` (новый)

Таблица `notifications`:
| Поле | Тип | Описание |
|------|-----|----------|
| id | int | PK |
| telegram_user_id | bigint | Кому отправить |
| message | text | Текст сообщения |
| scheduled_at | datetime | Когда отправить |
| status | enum | pending / sent / failed |
| attempts | int | Количество попыток (max 3) |
| created_at | datetime | Когда создано |

**Файл:** `app/services/notification.py` (новый)

Методы:
- `schedule_lesson_reminder(lesson)` — создать напоминание за 1 час до урока
- `schedule_lesson_confirmation(lesson)` — подтверждение бронирования

**Файл:** `app/api/v1/endpoints/internal.py`

Endpoints (только для бота, с `verify_service_key`):
- `GET /internal/notifications/pending` — уведомления готовые к отправке
- `PATCH /internal/notifications/{id}` — обновить статус

### Telegram Bot

**Файл:** `services/notification_fetcher.py` (новый)

Логика:
1. Цикл каждые 30 секунд
2. `GET /internal/notifications/pending`
3. Для каждого уведомления:
   - `bot.send_message(chat_id, text)`
   - `await asyncio.sleep(0.1)` — задержка для rate limit
   - `PATCH` статус на `sent`
4. При ошибке — логировать, попробует снова через 30 сек

### Миграция

```bash
alembic revision -m "add_notifications_table"
```

---

## Часть 4: Логирование

**Цель:** Понимать, что происходит в системе. Видеть ошибки без SSH на сервер.

### Backend

**Файл:** `app/core/logging.py` (новый)

Настройки:
- Формат: JSON (для будущего парсинга)
- Уровень: INFO для production, DEBUG для development
- Вывод: stdout (Docker соберёт)

Что логировать:
- Каждый HTTP запрос (метод, path, status, время)
- Ошибки с traceback
- Важные бизнес-события (урок забронирован, отменён)

**Файл:** `app/main.py`

Добавить middleware для логирования запросов.

### Telegram Bot

Аналогично:
- Каждая команда от пользователя
- Запросы к backend (URL, status, время)
- Ошибки

### Просмотр логов

```bash
# На сервере
docker compose logs -f api
docker compose logs -f bot
```

---

## Часть 5: Circuit Breaker + Cache (Telegram Bot)

**Цель:** Бот остаётся отзывчивым даже когда backend недоступен.

### Зависимости

```
pip install circuitbreaker cachetools httpx
```

### Структура

```
telegram/
├── services/
│   ├── backend_client.py    # HTTP клиент с Circuit Breaker
│   └── cache.py             # TTL кэш
```

### Cache

**Файл:** `services/cache.py`

```python
from cachetools import TTLCache

class BotCache:
    schedule = TTLCache(maxsize=100, ttl=300)      # 5 минут
    student_lessons = TTLCache(maxsize=500, ttl=120)  # 2 минуты
```

Правила:
- Кэшировать только READ операции
- После WRITE (бронирование, отмена) → инвалидировать кэш
- При недоступности API → отдать устаревшие данные (лучше, чем ничего)

### Circuit Breaker

**Файл:** `services/backend_client.py`

Параметры:
- `failure_threshold=5` — после 5 ошибок размыкаем
- `recovery_timeout=60` — через 60 сек пробуем снова

Логика:
1. Запрос к API
2. Успех → вернуть данные, сохранить в кэш
3. Ошибка → инкремент счётчика ошибок
4. 5 ошибок подряд → Circuit OPEN
5. Следующие запросы → сразу `CircuitBreakerError` (не ждём timeout)
6. Через 60 сек → один пробный запрос
7. Успех → Circuit CLOSED, работаем нормально

### Использование в хендлерах

```python
@router.message(F.text == "📅 Расписание")
async def show_schedule(message: Message):
    schedule = await backend_client.get_schedule(teacher_id=1)
    
    if schedule is None:
        await message.answer("⚠️ Сервис временно недоступен")
        return
    
    if schedule.get("_stale"):
        await message.answer(f"⚠️ Данные могут быть неактуальны\n\n{format(schedule)}")
    else:
        await message.answer(format(schedule))
```

---

## Часть 6: Бэкапы PostgreSQL

**Цель:** Не потерять данные при сбое.

### Скрипт

**Файл:** `scripts/backup.sh`

```bash
#!/bin/bash
BACKUP_DIR="/backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
CONTAINER_NAME="backend-db-1"  # или как у тебя называется

docker exec $CONTAINER_NAME pg_dump -U postgres lesson_manager | gzip > "$BACKUP_DIR/backup_$TIMESTAMP.sql.gz"

# Удалить бэкапы старше 7 дней
find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete
```

### Cron

```bash
# Каждый день в 3:00
0 3 * * * /path/to/scripts/backup.sh
```

### Восстановление

```bash
gunzip < backup_20260116_030000.sql.gz | docker exec -i backend-db-1 psql -U postgres lesson_manager
```

---

## Часть 7: Деплой

### Сервер 1 (Backend + Frontend)

**Структура:**
```
/opt/online-lesson-manager/
├── backend/
│   ├── docker-compose.yml
│   └── .env
├── frontend/
│   └── ...
└── scripts/
    └── backup.sh
```

**Docker Compose (production):**

Изменения для production:
- Убрать `--reload`
- Убрать volume mount исходников
- Добавить restart: always
- Настроить CORS на конкретный домен

**Reverse Proxy (nginx/caddy):**
- HTTPS терминация
- Проксирование на backend:8000
- Статика frontend

### Сервер 2 (Telegram Bot)

**Структура:**
```
/opt/telegram-bot/
├── docker-compose.yml
├── .env
└── src/
```

**Environment:**
```env
BACKEND_URL=https://api.example.com
BACKEND_API_KEY=ключ-из-части-1
TELEGRAM_BOT_TOKEN=токен-бота
```

### Обновление

**Простой вариант (пока без CI/CD):**

```bash
# На сервере
cd /opt/online-lesson-manager/backend
git pull
docker compose build
docker compose up -d

# Миграции
docker compose run --rm migrate
```

---

## Порядок реализации

```
Неделя 1:
├── Часть 1: Аутентификация (2-3 часа)
├── Часть 2: Health checks (1 час)
└── Часть 6: Бэкапы (1 час)

Неделя 2:
├── Часть 4: Логирование (2-3 часа)
└── Часть 3: Уведомления (4-6 часов)

Неделя 3:
├── Часть 5: Circuit Breaker + Cache (3-4 часа)
└── Часть 7: Деплой (2-4 часа)
```

---

## Нюансы и подводные камни

### Telegram Rate Limits
- Не более 30 сообщений в секунду
- Добавляй `await asyncio.sleep(0.05)` между сообщениями
- При 429 ошибке — экспоненциальный backoff

### Миграции при двух сервисах
- Сначала накатить миграцию
- Потом обновить backend
- Потом обновить бота
- Изменения схемы должны быть backward-compatible

### Часовые пояса
- Хранить в БД в UTC
- Конвертировать при отображении пользователю
- В настройках учителя хранить его timezone

### CORS
- В production указать конкретный домен frontend
- Не оставлять `allow_origins=["*"]`

### Секреты
- Не коммитить .env файлы
- Использовать разные ключи для dev/prod
- Периодически ротировать API ключи
